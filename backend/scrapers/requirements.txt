supabase>=2.0.0
python-dotenv>=1.0.0
playwright>=1.50.0
requests>=2.31.0
praw>=7.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Note: mcp-reddit is installed separately via 'pip install mcp-reddit'
# It's used as an MCP server, not a direct dependency
